#!/bin/sh

# apt-get update
# apt-get install -y curl

# curl -sfL https://get.k3s.io | sh -

# kubectl create deploy app-one --image=paulbouwer/hello-kubernetes:1.10
# kubectl expose deploy app-one --port=80

# kubectl create deploy app-two --image=paulbouwer/hello-kubernetes:1.10 --replicas=3
# kubectl expose deploy app-two --port=80

# kubectl create deploy app-three --image=paulbouwer/hello-kubernetes:1.10
# kubectl expose deploy app-three --port=80